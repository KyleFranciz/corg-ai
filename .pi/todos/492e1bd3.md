{
  "id": "492e1bd3",
  "title": "Refactor document ingestion script into class-based implementation",
  "tags": [
    "backend",
    "chroma",
    "ingestion"
  ],
  "status": "Todo",
  "created_at": "2026-04-27T21:28:28.938Z"
}

Refactor `backend/ingest.py` from procedural functions into a class-oriented design (e.g., `DocumentIngestor`) while preserving COR-18 behavior.

Current goals inferred from request:
- Keep support for `.txt`, `.md`, `.pdf`
- Preserve chunking defaults (1000/200)
- Preserve idempotent Chroma upsert behavior
- Preserve SQLite `Documents` and `Chunks` synchronization
- Keep summary reporting (processed/added/updated/skipped/failed)

Need clarification on the rest of the requirement (`"that instead ..."`) before implementation.
